def sample_patch(
        image,
        gt_boxes,
        min_size,
        max_size,
        min_iou,
):
    """

    Args:
        - image: The input image
        - gt_boxes: The ground truth boxes in the image
        - min_size: The minimum size of the patch
        - max_size: The maximum size of the patch
        - min_iou: The minimum iou with ground truth box to be considered as a patch

    Returns:
    """
    return ""
